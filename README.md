# food-cost

Food cost web app with a SQLite ingredient database and recipe costing form.

## Run

```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
python app.py
```

Open http://localhost:5000.
